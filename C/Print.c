#include<stdio.h>
void jannat()
{
    int n;
    scanf("%d",&n);
    int a[n];
    for (int i = 0; i < n; i++)
    {
        scanf("%d",&a[i]);
    }
    int min = TMP_MAX,max = TMP_MIN;
    for (int i = 0; i < n; i++)
    {
        if (a[i]==)
        {
            printf("%d",i);
        }
        else
        {
            printf("%d ",i);
        }
    }
    
}
int main()
{
    jannat();
    return 0;
}