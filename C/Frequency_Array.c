#include<stdio.h>
void free_count(int arr[],int n,int fre[])
{
    for (int i = 1; i <= n; i++)
    {
        int val = arr[i];
        fre[val]++;
    }
    
}
int main()
{
    int n,m;
    scanf("%d %d",&n, &m);
    int arr[n+1],fre[m+1];
    for (int i = 1; i <= n; i++)
    {
        scanf("%d",&arr[i]);
    }
    for (int  i = 1; i <= m; i++)
    {
        fr
    }
    
    
    return 0;
}