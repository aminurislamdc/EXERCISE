#include<stdio.h>
#include<string.h>
int main()
{
    char aj[101];
    scanf("%s",&aj);
    // int count = 0;
    // int ajj = strlen(aj);
    
    printf("%d",strlen(aj));
    return 0;
}